<resources>

    <!-- Базовый стиль для приложения. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

    <style name="ButtonStyle">
        <!-- Стиль для кнопки. -->
        <item name="android:background">@drawable/ripple</item>
        <item name="android:layout_marginEnd">@dimen/button_margin_end</item>
        <item name="android:layout_marginStart">@dimen/button_margin_start</item>
        <item name="android:layout_marginBottom">@dimen/button_layout_margin_bottom</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:fontFamily">Roboto medium</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textColor">@color/textColorButton</item>
    </style>

    <style name="TextStyle">
        <!-- Стиль для названия команды. -->
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/text_layout_margin_top</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="TextStyleScore">
        <!-- Стиль для счета команды. -->
        <item name="android:textSize">@dimen/score_size</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/score_margin_top</item>
        <item name="android:layout_marginBottom">@dimen/score_margin_bottom</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

</resources>
